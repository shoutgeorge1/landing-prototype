# Credentials (local only)

Place Desktop OAuth client JSON files here, for example:

```text
credentials/client_secret_DESKTOP.json
```

This directory is gitignored for secrets. Keep only this README and `.gitkeep` in source control.

Never store refresh tokens or `.env` files here in a committed state.
